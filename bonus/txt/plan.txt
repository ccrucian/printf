Parsing

faccio un tipo struct dove raccolgo tutte le opzioni
	con int minus e zero che sono == 1 se ci stanno;
	char   flag = {-, 0} se c'è - scalza 0
	int 	width = digits che passo prima ad atoi
	int	precision = digits dopo il . che passo ad atoi
*-Faccio una funzione che prende il puntatore alla struttura 
inizializza tutti i valori a 0

*-Faccio una funzione che dichiara una struttura, chiama set_opt per
inizializzarla e torna il puntatore alla struttura(non si puo fare \
perchè non si può ritornare il puntatore a una variabile statica)
	Quindi lo faccio in ft_parser (vedi dopo) dichiaro e\
	 inizializzo.

is_specifier dà 1 se il carattere non è secifier.

*-Faccio funzione ft_parser che vien chiamata dopo aver trovato % \
(deve prendere la stringa da dopo% quindi gli passo &s[i] e il\
puntatore alla struct), poi setto a zero la struct \
**"dichiara t_opt che è la struttura dove racolgo i dati di parsing"\
--> cambiato la struct la dichiaro nella funzione che chiama parser*\
e chiama semplicemente le varie funzioni parser, passandogli \
la stinga e il puntatoe alla struttura:
CHIAMA:
	parse_flag: prende la stringa e la scorre fino a quando non 
			trova specifier (is_specifier == 0)
			cerca 0 e - e setta i campi a 1 in opt
	parse_width: prende stringa e salva in opt->width
	parse_point_precision
	parse_specifier
ora li ho salvati tutti nella struct opt.


-una funzione che mi itera le opzioni dpo % cosi da non stamparle- 
